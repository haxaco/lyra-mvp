{
  "name": "lyra",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": "20.x" },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build:ui": "pnpm --filter @lyra/ui build",
    "build:sdk": "pnpm --filter @lyra/sdk build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "2.0.6",
    "typescript": "5.5.4"
  }
}